"""Record encodings e — JSON, canonical CBOR, MessagePack, delta-CBOR.

Encoding axis of the decision space (docs/01 §1 "Encodings e") behind a common ``Encoder``
ABC (docs/03 §3 encodings/). Stub at P0; encoders land in P1a.
"""
