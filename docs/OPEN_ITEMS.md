# Open items register — everything not yet closed
*Created 2026-07-28 by the pre-P8 full audit. **This is the single tracked list.** Anything
"remaining", "deferred", "assumed" or "future work" belongs here, not scattered in prose.
Status: `OPEN` · `DECIDED` (needs implementing) · `ACCEPTED` (a stated limitation, will not be fixed).*

Ordered by what a thesis examiner would hit first.

---

## A. Claims and framing

| # | Item | Status | Why it matters | Action |
|---|---|---|---|---|
| **A1** | Headline framing | **CLOSED 2026-07-29** | Reframed everywhere: paper abstract/results/conclusion, charter §3, narrative. Headline is now **total bytes −58.7 %** reported as a **decomposition** (placement×batching 79.2 %, encoding 20.8 %, scheme byte-neutral), and the load-bearing claim is the **feasibility envelope** (N_max 103 vs 32 vs 25). Two new paper tables + `tab:envelope`. Pinned by `test_headline_decomposition.py` | — |
| **A2** | b=4 is fixed by inputs, not discovered | **CLOSED 2026-07-29** | Stated plainly in the paper's attribution paragraph and docs/02 §7a, which shows b depends only on the product Λ·D_max | — |
| **A3** | `[VERIFY]` citations in `paper/main.tex` and `docs/TECHNICAL_NARRATIVE.md` §2 | OPEN — **deferred by Mohamed to start of P8** | Thematic positioning is right; exact references unconfirmed | One consolidated literature pass at P8 |
| **A4** | Autopilot stream rates | **CLOSED 2026-07-29** | PX4 confirmed at source (NORMAL 5, OSD/CONFIG 10, **ONBOARD 50** Hz). ArduPilot corrected: the default is **vehicle-specific** — Plane/Rover 1 Hz, Sub 3 Hz, **Copter 0 Hz** (on-demand), not a universal 1 Hz. Additionally anchored to **3GPP TS 22.125 R-5.2.2-010** (≥10 msg/s); Λ=20 is bracketed by both | — |
| **A5** | T6's 48 B signature floor | **CLOSED 2026-07-29** | Cited to **draft-irtf-cfrg-bls-signature-05** (Boneh et al.), `minimal-signature-size` = 48 B G1. **Correction: the draft states 126-bit security, not 128** — fixed in docs/02 T6 | — |

| **A6** | **T2a prior-art check — DONE 2026-07-30.** The search returned established results: batching amortizes per-transmission overhead with diminishing returns, and the latency trade-off is thoroughly covered by the packet-aggregation literature (alpha-beta cost model) | **CLOSED** | **T2a is analysis, not a novel theorem** — same disposition as T6 (F16). It is a correct and useful statement of *which ceiling binds*, and it is presented that way; no novelty is claimed | Nothing. A3 still confirms the citation formatting |
| **A7** | **External baseline — RESOLVED for the LoRa arm (F20), argued for the 802.11 arm.** LoRa: Bor et al. 2017's measurement-fitted model is now **implemented and run at our operating point** (`make exp-lora-external`) — it gives **N_max = 4** against our **5**. 802.11: already validated against Bianchi and Ma & Chen. TBRD/TESLA was moved to §Related Work because it offers **no non-repudiation** and so is not a substitute | **CLOSED (LoRa) / ACCEPTED (auth schemes)** | The remaining gap is a measured comparison against another *authentication* scheme. The honest candidate is a VANET CLAS scheme, not TESLA | Optional, needs Mohamed's call on scope |

## B. Unreferenced or assumed constants

| # | Item | Status | Bias | Action |
|---|---|---|---|---|
| **B1** | H_f | **CLOSED 2026-07-29** | **Measured = 44 B** from `placement/wire.py` (was an assumed 40). Placement-dependent (A 45→51, D 81); the flat 44 B is conservative for both. Headline unmoved; b_max 31→30; total cut 58.30→58.68 % | — |
| **B2** | N_local | **CLOSED 2026-07-29** | Reported as a **curve**, not an assumption: N_max = **103** (co-design) vs **32** (A+CBOR) vs **25** (A+JSON). N=50 is quoted because it lies between — a swarm the baselines cannot serve and the co-design can. New `ENVELOPE` rows in `capacity_envelope.csv` | — |
| **B3** | **RESOLVED 2026-07-30 (Mohamed): report the operating region, adopt the compliant point.** Primary is now **Λ=50 Hz, D_max=100 ms** — PX4 `MAVLINK_MODE_ONBOARD` rate, fully TS 22.125 compliant. The (20 Hz, 250 ms) point is retained as a reported alternative | **CLOSED** | Bytes are **identical** (b ≤ Λ·D_max ⇒ both give b=4, 72.0 B/rec, −58.68 %). Compliance costs **~2× swarm** (233→116 at V≥0.95). ⚠️ Floor found: b≥4 under 100 ms needs **Λ ≥ 40 Hz**; below that b=1 and the saving collapses to 12.2 % | Done — paper, docs/01, TRADEOFFS and the envelope table all restructured around the region |
| **B4** | Loss grid p | **CLOSED 2026-07-29** | Justified by **mechanism**: 802.11 broadcast carries no ACK and is never retransmitted, so the receiver sees the raw channel error rate. TS 22.125 Table 7.2-1's 99.9 % is for *managed* C2 links with ARQ — our grid is 20–100× more pessimistic (conservative). **T6 needs only ε ≤ p**, so it holds across the whole grid | — |
| **B5** | **K = 16** delta keyframe interval — fixed, never optimized | ACCEPTED (docs/01 §1 says so) | Affects delta's 45 B | Stated as out of scope; re-check at P8 that the thesis says this |
| **B6** | MTU = 1500, 802.11a PHY constants (20/16/9/34 µs, 6 Mb/s, 36 B MAC) | **CLOSED** | — | All standard 802.11a values; MAC overhead cross-checked against NS-3 3.41 (D9/audit A1) |
| **B7** | LoRa constants (SF/BW/payload/sensitivity/duty cycle) | **CLOSED** | — | Every value transcribed from SX1276 Rev.7 and RP002-1.0.3 with section numbers |

| **B8** | Medium-exclusion finding | ⚠️ **CLOSED 2026-07-29 — promoted to T7, then T7 WITHDRAWN the same day** | T7 claimed capacity excludes at **U ≥ 1**. Its own validation experiment (D3) refuted it: NS-3 delivers **98.8 % at U = 1.00**, and the V≥0.95 crossing is at **U ≈ 2.80**, so the 3GPP-compliant point (U=1.39) is feasible and **the 75 % cut is achievable at the deadline**. T7 is withdrawn; the surviving mechanism is a **cost** — meeting the 100 ms bound halves the swarm (N≤233 → N≤116) and changes nothing in bytes | Lesson recorded in docs/02: the claim was published before the experiment already scheduled to test it |

## C. Model scope — what the models do not cover

| # | Item | Status | Action |
|---|---|---|---|
| **C1** | DCF channel-access delay absent from D(b) | **CLOSED 2026-07-29 — MEASURED, and it is negligible** | NS-3 non-saturated sweep (`ns3_delay.csv`): the omitted access delay is **+0.033 ms** at the reference point and **+2.2 ms even at 9× saturation load**, against a 250 ms budget. D(b) is validated. The structural reason: **802.11 broadcast has no ARQ and no queue buildup**, so overload degrades *delivery*, never latency |
| **C2** | Saturation models at a non-saturated point | **PARTLY CLOSED 2026-07-29** | Measurement shows the consequence precisely: saturation throughput **understates usable capacity by ≈2.8×** (98.8 % delivery still at U=1.00; V=0.95 crossing at U≈2.80). So U is a **conservative admission metric, not a feasibility boundary** — the envelope figures computed at U<1 are lower bounds, and the comparative ratio is unaffected since all are computed alike |
| **C3** | **No mobility, no spatial reuse, no hidden terminals** — single collision domain | ACCEPTED, in the paper's limitations | Real formations get both spatial reuse (helps) and hidden terminals (hurts); neither is modelled |
| **C4** | **Loss is i.i.d. per frame**; real FANET loss is bursty | PARTLY CLOSED | F11 proved V_D ≤ V_B under *any* correlation (joint ≤ marginal), so the **T3 ordering survives**. Absolute V values still assume independence |
| **C5** | **No sender-side CPU throughput constraint** in the optimizer | ACCEPTED | Verified never to bind: worst case (BLS inline) is 13.9 % of one core; it would bind at ~360 rec/s vs our 20 |
| **C6** | Key compromise, consensus, multi-hop routing | ACCEPTED — charter scope | — |

## D. Validation gaps

| # | Item | Status | Action |
|---|---|---|---|
| **D1** | End-to-end energy validation | **CLOSED 2026-07-29 — measured, then the model was FIXED** | Initially ~32 % low. Root-caused to D7 (no chain-hash term) + D6 (`p_cpu_w` from isolated primitives). Both fixed; **residual is now +7.5 % to +14.3 %** across four configurations, all of it frame assembly. The interim "overstates the optimized advantage" claim is **retracted** — measured advantage 2.035× vs predicted 1.985× | Energy figures are **lower bounds by ~10–14 %** (uncharged CPython framing). Deliberate: charging it would model our prototype, not the design |
| **D2** | LoRa arm had no measurement of any kind | **CLOSED 2026-07-29 — now simulated** | NS-3.48 + signetlabdei LoRaWAN module gives the multi-node capacity envelope, the one LoRa quantity that is not analytically derivable: **N_max = 5** at DR5 (V≥0.95), a sharp ALOHA cliff (1.0000 at N=5 → 0.8656 at N=8). Combined with the 121× per-node rate gap this is **≈2500× less aggregate capacity** than the 802.11 arm. Still **not hardware**: no LoRa radio metered, no energy column — the chapter must say "simulated bound", not "measured" |
| **D3** | NS-3 validated throughput only | **CLOSED 2026-07-29** | New scenario `ns3/authbc-delay.cc` + `make sim-ns3-delay` validate **delivery delay and delivered fraction** under non-saturated load (the saturated scenario cannot measure delay — an always-backlogged queue diverges by construction). Artifact `results/raw/ns3_delay.csv`. Energy is separately validated end-to-end by D1 |
| **D4** | Hardware is **2× RPi4** (charter said 4×; `hw/SETUP.md` records the real inventory) | **CLOSED 2026-07-28** | Charter corrected |
| **D6** | `p_cpu_w` methodology | **CLOSED 2026-07-29 — and the premise was wrong** | The question was *"is p_cpu_w configuration-dependent?"*. Metered on all four E5 configurations: **0.732 / 0.744 / 0.755 / 0.760 W — a 3.8 % spread**, so **no**. What was wrong is that 0.634 W came from *isolated primitives* and understates any composed pipeline by **18.2 %**. Adopted **0.749 W** (median of four composed pipelines); one constant keeps the model predictive. Artifact `results/hw/energy/p_cpu_w_composed.md` |
| **D7** | ARM SHA-256 timing / chain-hash term | **CLOSED 2026-07-29** | Measured on authbc-pi4a with the P1 harness: **2745.5 ns** (45 B = prev_hash 32 + delta body 13). Added to `Measured.t_hash_s` and charged **2× per record** (sender extends the chain, receiver verifies it) — it does **not** amortize over b. E5 optimized energy 112.08 → **115.56 µJ/rec**, exactly the predicted +3.481 (then → **131.68** once D6's composed-pipeline power landed — that is the current figure). Bytes untouched. Artifact `results/hw/p1_hash.authbc-pi4a.csv`; pinned by `test_chain_hash_term.py` |
| **D5** | RPi3 / BeagleBone cross-platform points | OPEN — optional | "Secondary/stretch" tiers in `hw/SETUP.md`; only add generalization breadth |

## E. Process and reproducibility

| # | Item | Status | Action |
|---|---|---|---|
| **E1** | `lora_eu868.csv`, `lora_codesign.csv`, `capacity_envelope.csv` were **outside the frozen gate** | **CLOSED 2026-07-28** | Added to `_CASES`; gate is now 13 tests and all three re-derive byte-identically |
| **E2** | docs/03 and docs/07 stale | **CLOSED 2026-07-29** | Both marked **HISTORICAL** with a banner pointing at DECISIONS / OPEN_ITEMS / TECHNICAL_NARRATIVE |
| **E3** | Parallel-lane status files | **CLOSED 2026-07-29** | Marked historical, and annotated that D7 resolved to serial — the lanes were never used |
| **E4** | docs/04 §3 / docs/06 §2 wrong NS-3 references | **CLOSED — were already correct** | Checked 2026-07-29: both already say 10 s × 10 seeds, PacketSocket+PacketSink (not FlowMonitor), and `ns3/parse_ns3.py`. The DECISIONS entries saying "to be amended at P8" were themselves stale |
| **E9** | **ANSWERED by simulation (F25).** Ran `--gwRegion=eu` (3 channels, 8 demodulation paths) as the infrastructure-variant comparison | **CLOSED** | **N_max = 8** (predicted ≥14 — prediction wrong). The gateway preset buys ~**2.68× aggregate delivery at N=50** but only **5 → 8** at the V≥0.95 threshold, because that threshold sits on a near-vertical part of the curve. Same capacity-vs-reliability duality as the 802.11 arm's U<1 vs V≥0.95 | Done. `results/raw/lora_capacity_eu.csv` |
| **E22** | **CLOSED (F30): re-measured at 30 seeds and the frozen matrix promoted.** Unicast **+1.29/−0.40 %** confirms the published band; broadcast's **−1.44 % endpoint was sampling noise** and tightens to **−0.51 %** | **CLOSED** | Worst SE now ±0.13 % (unicast) / ±0.39 % (broadcast) against bands an order of magnitude larger. The 10-seed file is kept as `ns3_matrix_SUPERSEDED_lowseed.csv` | Done — paper, docs/02 and the 19 band tests all re-baselined |
| **E23** | ⚠️ **CLOSED (F30), and it moved a headline.** The V=0.95 crossing was read off 5 seeds at U≈2.797; at 30 seeds it is **U = 2.435** | **CLOSED** | Capacity at V≥0.95 becomes **31→100** (compliant) and **88→213** (relaxed), down 10–14 %. **The co-design ratio barely moves** (3.29→3.23, 2.26→2.42) — the claim is the ratio and it survives | Done — paper quotes 213/100 and U≈2.44; `ns3_delay.csv` promoted, old kept as `_SUPERSEDED_lowseed` |
| **E24** | **CLOSED (F30): small-frame sweep re-run at 30 seeds.** Broadcast holds to **±0.21 % across 72–288 B**, tighter than at 1400 B — F29 survives a 10× sample | **CLOSED** | ⚠️ Unicast's **−1.40/−2.60 % bias at 72 B is CONFIRMED real** (SE ±0.06 %), not noise. Does not touch the headline (the co-design runs on broadcast), and is now **stated in the abstract** rather than absorbed | Done |
| **E21** | **NEW (F29): the channel model was validated at 1400 B only, but applied at 72–288 B** — a different regime, where per-frame overhead dominates instead of payload | **CLOSED — measured, and favourable** | Broadcast (which `N_max` depends on) agrees to **±0.36 % across 72–288 B, tighter than the ±1.44 % at 1400 B**. ⚠️ Unicast carries a **systematic −1.5..−2.4 % bias at 72 B**, outside its published band — a bias, not scatter, plausibly the anomalous-slot effect. Does not touch the headline (co-design runs on broadcast) | Quote the unicast band **as measured at 1400 B**, not as general. `ns3_matrix_72B.csv`, `ns3_matrix_288B.csv` |
| **E17** | ⚠️ **NEW (F27): "V ≥ 0.95" means three different things.** (i) optimizer constraint `V = 1 − p` with **p = 0.05 assumed** — an *input*; (ii) the *measured* NS-3 boundary at U ≈ 2.80; (iii) LoRa `delivered_frac`, *measured* | ⚠️ **OPEN — wording, not arithmetic** | For placements A/B/C, `verifiability()` returns `1 − p` **independent of encoding, batch and scheme**, so the "keep V ≥ 0.95" half of the pre-registered criterion is satisfied **by construction with zero margin**. It can only fail for D. The ≥40 % byte cut is the genuinely falsifiable half | Say so in the paper: distinguish the assumed constraint from the two measured quantities, and note that the byte cut carried the falsifiability |
| **E18** | ⚠️ **NEW (F27): two `Placement` enums with shared member names.** `models.energy.Placement` (StrEnum) vs `placement.wire.Placement` (IntEnum, values are frozen wire format). Seven `is Placement.X` comparisons fail **silently** across them | **PARTLY FIXED** | `verifiability(wire.Placement.D, 4, 0.05)` returned 0.95 instead of 0.8145 — a 17 % overstatement of block-level loss robustness, no error raised. Latent in `src/` (mypy covers it, `experiments.py` imports correctly) but **it caught me in ad-hoc analysis this session** | `verifiability()` now raises `TypeError` naming both modules, with a regression test. ⚠️ **The other six comparisons are still unguarded** — either guard them too or unify the enums |
| **E19** | **NEW (F27): the published validation bands were asserted nowhere.** The abstract's +1.28/−0.49 % and 2.49 % were reproducible but untested | **FIXED** | Recomputed from frozen data: unicast band is **exactly** +1.28/−0.49 %. ⚠️ Two clarifications: the convention is **(sim−model)/model** (the reverse gives −1.26/+0.49, same magnitudes opposite signs), and the **2.49 % is success-probability, not goodput** — goodput agrees to 1.44 % | `tests/test_validation_bands.py`, 16 tests, incl. the F9 result that the naive Bianchi-to-broadcast reduction is badly optimistic |
| **E20** | **Mobility — DEFERRED by Mohamed (2026-07-30), plan agreed.** All scenarios static; at 20 m/s the channel decorrelates *within* a frame (LoRa: 7.3 ms coherence vs 364 ms ToA ⇒ ~50 fades/frame) and a node covers 72 km per run | **PLANNED — do not start yet** | **Direction: separate NEW scenario files, not edits**, so the frozen static results stay reproducible and mobility becomes an additive comparison. **Preceded by a deep literature survey** of the mobility models, speeds, node counts, areas and durations FANET/UAV papers actually use — that survey is the reference we test against | Full plan in **`docs/MOBILITY_PLAN.md`** (§6 has the execution shape). Verdict stands: **not for the 802.11 arm** (Bianchi/Ma&Chen have no position term), LoRa only, **after E13** |
| **E13** | **FIXED (F28): `N_max` corrects from 5 to 3.** One-sided `JitteredSender` (never earlier than the duty interval, so the 1 % limit holds) + seeds raised 3→30 + min/max/σ now emitted | **CLOSED** | The old 5 was seeds 1–3 landing on 1.0000 by luck. A 30-seed **no-jitter control also gives 3**, so the correction is the sampling; jitter fixes the *shape* (tail at N=5 lifts 0.20→0.70). ✅ **Mean deviation from closed-form periodic ALOHA drops 0.0688 → 0.0059, a 12× improvement** — and 3 is exactly what theory predicts. Three lines now agree: our sim 3, theory 3, Bor et al. 4. Bonus: duty was **exactly 1.0000 %** (EU868 needs **<** 1 %); jitter makes it 0.9864 % | Done. `lora_capacity.csv` regenerated; 3-seed file kept as `_SUPERSEDED` |
| **E14** | ⚠️ **NEW (F26): our LoRa runs have NO capture** — `LoraInterferenceHelper::ALOHA` is `+inf` on the same-SF diagonal, so every co-SF overlap is fatal. I had claimed capture explained our low-N behaviour in F19/F20/F25, the register and the paper — **all wrong** | **CORRECTED in text; the configuration itself is a defensible conservative choice** | Real explanation for beating `e^(−2G)`: traffic is **periodic, not Poisson** — `(1−2τ/T)^(N−1)` = 0.868 vs 0.8656 measured at N=8. Goursaud (6 dB capture) would add **+3.3 pts at N=8 and 1.36× at N=50**, so we are conservative by that much | Keep the no-capture matrix (conservative, and defensible for a worst case) but **say so**. `--interference` flag added; `lora_capacity_goursaud.csv` quantifies the gap |
| **E15** | **NEW (F26): nothing moves.** All four scenarios use `ConstantPositionMobilityModel` — no Doppler, no link churn, no time-varying distance, in a *Flying* ad-hoc network study | **ACCEPTED as a limitation, stated** | Defensible for the 802.11 saturation arm (MAC contention in one collision domain is mobility-independent to first order while nodes stay in range). A real gap for the LoRa arm, where F23 showed range dominates | State in the paper. Mobility would be a separate study |
| **E16** | **NEW (F26): latent defects in the drivers.** (a) `n_max` takes the *last* passing N, not the first failure — no `break` in the sweep; (b) **no variance is reported anywhere** (no σ, CI or min/max, only 3-seed means); (c) `sent` counts PHY `StartSending`, so MAC-layer drops are invisible | **(b) is the root cause of E13; (a) and (c) are latent** | (a) harmless on monotone curves, wrong under the noise E13 documents. (c) **verified not biting**: `sent` is identical across presets and matches `n × 3600/36.378` analytically | Fix (a) with a `break`; make the drivers emit σ and min/max; add a guard for (c) if `appPeriod` is ever reduced |
| **E12** | **RESOLVED by simulation (F25): the simulator CANNOT produce the measured link loss, so the range qualifier stands on evidence rather than assertion.** Ran `--channelModel=shadowing` at r=1000 and r=500 | **CLOSED as a stated limitation** | Results **identical to `ideal` on every row**. Verified not to be a wiring bug: at r=4000 shadowing takes delivery 1.000 → 0.515. Cause is link margin — **28.8 dB at 500 m, 17.5 dB at 1000 m** (LogDistance n=3.76, 14 dBm TX, −130 dBm SF7 sensitivity); an ~8 dB σ cannot erode that, and simulated failure onset is ≈4200 m. ⇒ our propagation parameters describe a much better link than a real drone-to-drone channel | Composition is the honest treatment and is implemented: simulation gives `P_nocoll(N)`, hardware gives `P_link(range)`, `exp-lora-external` multiplies them. **Future work:** recalibrate the path-loss exponent against Zirak Table I — deliberately NOT done, since fitting to 9 points from one campaign trades a stated limitation for a hidden one |
| **E10** | ⚠️ **NEW (F21): two ad-hoc effects the star simulation cannot show.** (a) **Half-duplex** — a LoRa radio cannot receive while transmitting, so each peer is blind ≈1 % of the time and misses ≈2 % of any peer's frames; a gateway never suffers this. (b) **Full replication** — `V` is per-receiver, but a record reaching *every* peer in one broadcast is ≈`p^(N-1)` = **0.815** at p=0.95, N=5 | **STATED as scope, not modelled** | Both are inherent to the star topology, and LoRaWAN has no peer-to-peer mode to simulate instead. Full replication in one hop is an application-layer concern (gossip/relay), explicitly out of scope — but the paper must say so rather than let "decentralised" imply it | Stated in the low-rate section. Modelling raw-LoRa peer-to-peer would be a separate study |
| **E11** | ⚠️ **NEW (F21): the `ALOHA` preset does NOT enforce the 1 % duty cycle** — it sets the g1 sub-band to duty cycle **1 (100 %)**. Our 1 % offered load comes from `appPeriodS = 38.4` s at the application layer instead | **CORRECT but was undocumented** | Self-consistent (38.4 s × 1 % ≈ the 384 ms frame), and it matches Bor et al., who also drive at the ceiling. ⚠️ **Anyone changing `appPeriod` without changing the preset would silently violate the regulation the whole LoRa arm rests on** | Documented in the scenario header and in F21. Consider asserting the implied duty cycle at run time |
| **E8** | ⚠️ **NEW: `N_max = 5` is a within-one-SF bound, not a LoRaWAN network capacity.** Because the data rate is a design variable, each run fixes one DR, forfeiting the SF quasi-orthogonality that supplies much of a real deployment's capacity | **ACCEPTED as a limitation, stated** | Surfaced by corroborating against Bor et al. 2017 (F18), which also *validated* the number: their 1000-node point equals ~40 of our nodes in offered load, where they see ~90 % collisions and we see 59–75 %. **Our simulation is slightly more optimistic than their measured model, so the low count is not an artifact** | Stated in the low-rate chapter and `docs/literature/README.md` §5. A multi-SF run would answer a different question (deployment capacity) than the one we ask (per-configuration feasibility) |
| **E7** | **mypy — DONE 2026-07-30, 0 errors across 49 files.** All 18 fixed at the design level, none suppressed. Three were real defects (F17): an LSP violation across the whole `Framer` hierarchy, BLS-only methods called through the general protocol, and three dead `type: ignore`s | **CLOSED** | `make typecheck` is now in `make all` and pre-commit, with `warn_unused_ignores` on so dead suppressions cannot accumulate | Nothing |
| **E5** | `bench/micro.py` coverage | **CLOSED 2026-07-29** | "Measurement plumbing" failed as a justification: two bugs were introduced into `measure_crypto` on 2026-07-29 and **both were caught by hardware, not tests** (a missing keyword-only arg, and a row reporting `msg_bytes=200` for a 45 B input — a silent provenance error). `test_micro_rows.py` now pins the row contract, the per-op input sizes and the schema |
| **E6** | Missing figures | **CLOSED 2026-07-29** | `analysis/figures_envelope_lora.py` produces `fig_envelope.png` (N_max per config), `fig_t6_exclusion.png` (T6 tiers over EU868) and `fig_lora_chain.png` (F5). Visually checked, not just generated |

## F. Carried decisions needing implementation

| # | Item | Status | Action |
|---|---|---|---|
| **F1** | Paper restructure | **CLOSED 2026-07-29** | Done in the decided shape: T6 added as theory (§III-F), new §VI "Generalisation: the Low-Rate Regime" (T6 on EU868 + chaining + the N_max=5 capacity result), Results reordered feasibility-first. 7 pages, builds clean. Fixed three stale claims en route (T5's MTU wording, the missing chain-hash term, E4's x86 ratio) and caught one repeat of the retracted T7 error before it shipped |
| **F2** | Abstract/conclusion framing | **CLOSED 2026-07-29** | Rewritten in the A1 pass: abstract leads with total bytes + decomposition + the feasibility envelope; conclusion states the `1−1/b` result explicitly. One residual: the success criterion is still phrased as an *auth-byte* target (§I), which is fine — it is the pre-registered criterion and must not be moved after the fact |

---

## What is genuinely settled
Freshness enforced as constraint + objective (F10) · Ma & Chen broadcast model, cited, ≤0.36 % vs
NS-3 (F9) · NS-3 sink lifetime (F8) · OFDM-quantised airtime (D9) · 30-seed size protocol (F4) ·
BLS = 96 B everywhere (F1) · measured powers and ARM timings (P7b) · T2a binding-ceiling theory ·
F11 loss-correlation ordering · F12 aggregate-vs-per-node arrival · T6 · F5 on LoRa · LoRa PHY
constants · the frozen-staleness gate.
